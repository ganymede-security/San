package config

// Defines the middleware to be used
type ServerMiddleware struct {
	// TODO Add Logging, Session, Context middleware
	// The Logger to use. Default is the logger wrapper
	// created in the "log" package. Any standard logger should be suitable
	//Logger
	//Context
	//Session
}